# PNI SC 0424 开发日志

- 日期: 2026-04-24
- 项目: PNI SC
- 记录人: Codex

## 今日目标
- 对齐并增强 `02.3_myeloid_subclustering.ipynb`，复用 `02.2` 的 Section 6 设计。
- 完成 no-P02 high/low 流程的一致化与可复现性改进。
- 新增髓系 cluster 5 双细胞风险复核并更新最终注释策略。

## 主要改进

### 1. 路径与环境可复现性
- 更新 `tmp/make_no_p02_highlow_notebooks.py`:
  - 移除硬编码根目录，默认按脚本路径推断仓库根目录。
  - 新增 `--root` 参数。
  - 新增环境变量 `PNI_SC_ROOT` 覆盖路径能力。
  - notebook 内 `Config.BASE_DIR` 改为自动解析。
- 新增 `requirements.txt`（最小依赖清单）。
- 更新 `README.md`，补充最小环境安装和 notebook 变体生成方式。

### 2. 02.3 与 Section 6 对齐
- 将 `02.2_subclustering_annotation_noP02_highlow.ipynb` 的 Section 6 相关设计迁入 `02.3_myeloid_subclustering.ipynb`。
- 保留关键分群参数、marker panel 与作图风格一致性。
- 保留并可复用 no-P02 high/low 输入输出路径设定。

### 3. 修复 notebook 标题乱码
- 对 `02.3_myeloid_subclustering.ipynb` 的 markdown 标题进行多次落盘校验修复。
- 当前标题行不再包含乱码占位符（`?` / `�`）。

### 4. 新增双细胞复核（Cluster 5）
- 在 `02.3_myeloid_subclustering.ipynb` 新增 `Section 6B-3 - Doublet projection check (Cluster 5)`。
- 自动识别并使用双细胞字段：
  - score: `doublet_score`（回退 `scrublet_score`）
  - flag: `predicted_doublet`（回退 `is_doublet`）
- 新增图表与导出：
  - UMAP 投射（`leiden` / doublet score / doublet flag）
  - 全 cluster 的 doublet score violin
  - `cluster 5 vs rest` violin
  - `cluster 5 vs rest` predicted doublet rate 柱状图
  - 汇总表: `results/tables/integration_noP02_highlow/myeloid_doublet_summary_noP02_highlow.tsv`

### 5. 更新髓系注释策略（最终版）
- 按最新决策更新 `Section 6C`:
  - `0 -> Neutrophils/MDSC`
  - `1 -> FCN1+ Monocytes`
  - `2 -> C1QC+ TAMs`
  - `3 -> Mast cells`
  - `4 -> pDC`
  - `5 -> Doublet`（并在后续分析中剔除）
- 同步更新 `subtype_order`，保证后续 UMAP/dotplot/violin 展示顺序一致。

## 变更文件
- `README.md`
- `requirements.txt`
- `tmp/make_no_p02_highlow_notebooks.py`
- `notebooks/02_integration_annotation_noP02_highlow.ipynb`
- `notebooks/02.2_subclustering_annotation_noP02_highlow.ipynb`
- `notebooks/02.3_myeloid_subclustering.ipynb`

## 验证
- 命令: `python -m unittest discover tests`
- 结果: `Ran 11 tests ... OK`

## 备注
- 本次改动聚焦于 no-P02 high/low 分析链路与髓系子群注释稳定性。
- 若需刷新图和导出结果，请从 `02.3` 的 `Section 6B-3` 与 `Section 6C` 开始顺序重跑。
